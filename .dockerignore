# Installed fresh in the deps stage; the host copy is the wrong platform.
node_modules
.next

# Never bake secrets or local state into an image.
.env
.env.*
!.env.example
.claude

.git
.gitignore
Dockerfile
docker-compose*.yml
README.md
npm-debug.log*
.DS_Store
coverage

# local PostgreSQL data directory
.localdb
